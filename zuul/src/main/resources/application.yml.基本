server:
  port: 34100
eureka:
  client:
    service-url:
      defaultZone: http://laowang:xiaomei@localhost:8000/eureka
spring:
  application:
    name: 16zuul
management:
  endpoints:
    web:
      exposure:
        include: '*'